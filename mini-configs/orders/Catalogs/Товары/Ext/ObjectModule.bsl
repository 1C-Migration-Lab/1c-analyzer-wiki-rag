Процедура ПередЗаписью(Отказ)
	// Проверка заполнения обязательных реквизитов
	Если НЕ ЗначениеЗаполнено(Наименование) Тогда
		Отказ = Истина;
		Сообщить("Не заполнено наименование товара!");
		Возврат;
	КонецЕсли;
	
	// Проверка корректности цены
	Если Цена < 0 Тогда
		Отказ = Истина;
		Сообщить("Цена товара не может быть отрицательной!");
		Возврат;
	КонецЕсли;
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	// Дополнительные действия при записи
	Если ЭтоНовый() Тогда
		// Логика при создании нового товара
	КонецЕсли;
КонецПроцедуры
